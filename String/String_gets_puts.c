#include<stdio.h>
#include<string.h>

int main(int argc, char const *argv[])
{
    char name[20];
    printf("Enter The Name: ");
    gets(name);
    printf("entered name is: ");
    puts(name);
    return 0;
}
